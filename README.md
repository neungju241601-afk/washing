# 🧺 기숙사 세탁기 예약 시스템

고등학교 기숙사에서 사용하는 세탁기 예약 및 관리 시스템입니다. 세탁기 이용 시 발생하는 대기 시간 문제와 세탁 완료 후 제때 가져가지 않아 발생하는 시간 지연 문제를 해결하기 위해 개발되었습니다.

## 🚀 빠른 시작

### 🌐 바로 사용하기 (배포된 버전)

**Render에서 배포하기** (추천! 5분 안에 완료):
[![Deploy to Render](https://render.com/images/deploy-to-render-button.svg)](https://render.com/deploy?repo=https://github.com/neungju241601-afk/washing)

**Railway에서 배포하기**:
[![Deploy on Railway](https://railway.app/button.svg)](https://railway.app/template/new?template=https://github.com/neungju241601-afk/washing)

### 💻 로컬에서 실행하기

```bash
# 저장소 클론
git clone https://github.com/neungju241601-afk/washing.git
cd washing

# 패키지 설치
pip install -r requirements.txt

# 실행
python app.py
```

브라우저에서 `http://localhost:5000` 접속

## 📚 문서 안내

- **초보자를 위한 설명서**: [`간단한_설명서.md`](간단한_설명서.md) - 초등학생도 이해할 수 있는 쉬운 설명
- **GitHub 업로드 가이드**: [`GITHUB_업로드_가이드.md`](GITHUB_업로드_가이드.md) - GitHub에 올리는 방법
- **배포 가이드**: [`DEPLOYMENT.md`](DEPLOYMENT.md) - 무료 호스팅으로 배포하는 방법
- **자동 배포 가이드**: [`배포_자동화_가이드.md`](배포_자동화_가이드.md) - Render 배포 단계별 가이드

## 📋 목차

- [프로젝트 개요](#프로젝트-개요)
- [주요 기능](#주요-기능)
- [시스템 설계](#시스템-설계)
- [설치 및 실행](#설치-및-실행)
- [사용 방법](#사용-방법)
- [파일 구조](#파일-구조)
- [기술 스택](#기술-스택)
- [확장 가능성](#확장-가능성)

## 🎯 프로젝트 개요

### 문제 상황
- 기숙사 세탁기 이용 시 대기 시간 발생
- 세탁 완료 후 제때 옷을 가져가지 않아 다른 학생들이 기다려야 하는 상황
- 세탁기 상태를 확인하기 위해 직접 확인해야 하는 불편함

### 해결 방안
- 실시간 세탁기 상태 확인 시스템
- 스마트 예약 시스템으로 대기 순서 관리
- 세탁 완료 시 자동 알림 기능
- 웹 기반 직관적인 사용자 인터페이스

## ✨ 주요 기능

### 1. 세탁기 이용 현황 표시
- 세탁기별 상태 실시간 표시 (사용 가능 / 사용 중 / 완료)
- 사용 중일 경우 남은 시간을 분 단위로 표시
- 전체 세탁기 현황 요약 정보 제공

### 2. 스마트 예약 시스템
- 모든 세탁기가 사용 중일 경우 대기 예약 가능
- 예약자는 순서대로 관리 (FIFO - First In First Out)
- 5분 내에 세탁을 시작하지 않으면 예약 자동 취소
- 세탁기가 비면 대기 순서대로 자동 할당

### 3. 세탁 종료 알림 기능
- 세탁이 끝나면 사용자에게 자동 알림
- 카페의 진동벨 시스템과 유사한 개념
- 알림 확인 후 삭제 가능

### 4. 사용자 인터페이스
- 웹 기반 직관적인 UI
- 반응형 디자인으로 모바일에서도 사용 가능
- 실시간 상태 업데이트 (5초마다 자동 갱신)

## 🏗️ 시스템 설계

### 아키텍처

```
┌─────────────────┐
│   웹 브라우저    │
│  (사용자 인터페이스) │
└────────┬────────┘
         │ HTTP 요청/응답
┌────────▼────────┐
│   Flask 앱      │
│   (app.py)      │
└────────┬────────┘
         │
┌────────▼────────┐
│  시스템 로직     │
│(washing_system.py)│
└────────┬────────┘
         │
┌────────▼────────┐
│  데이터베이스    │
│  (database.py)  │
└────────┬────────┘
         │
┌────────▼────────┐
│  SQLite DB      │
│(washing_machine.db)│
└─────────────────┘
```

### 데이터 저장 방식
- **SQLite 데이터베이스**: 모든 세탁기 상태, 예약, 알림을 영구 저장
- **스레드 안전**: 다중 사용자 동시 접근 지원
- **자동 복구**: 서버 재시작 후에도 모든 데이터 유지
- **무료**: 추가 비용 없이 사용 가능

### 핵심 클래스

#### 1. `Machine` 클래스
- 개별 세탁기를 나타내는 클래스
- 상태 관리 (사용 가능 / 사용 중 / 완료)
- 사용자 정보 및 시간 관리

#### 2. `Reservation` 클래스
- 대기 예약 정보 관리
- 예약 만료 시간 자동 체크

#### 3. `WashingMachineSystem` 클래스
- 전체 시스템 관리
- 세탁기 상태 업데이트
- 예약 및 알림 처리
- 데이터베이스 연동

#### 4. `Database` 클래스
- SQLite 데이터베이스 관리
- 스레드 안전한 데이터 접근
- 영구 데이터 저장

### 데이터 흐름

1. **세탁 시작**
   ```
   사용자 요청 → 사용 가능한 세탁기 확인
   → 있으면: 바로 시작
   → 없으면: 대기 예약 생성
   ```

2. **세탁 완료 처리**
   ```
   백그라운드 스레드가 주기적으로 상태 확인
   → 시간이 지나면 완료 상태로 변경
   → 사용자에게 알림 전송
   ```

3. **옷 가져가기**
   ```
   사용자 확인 → 세탁기 리셋
   → 대기 예약이 있으면 다음 사용자에게 자동 할당
   → 다음 사용자에게 알림 전송
   ```

## 🚀 설치 및 실행

### 필요 조건
- Python 3.7 이상
- pip (Python 패키지 관리자)

### 설치 단계

1. **저장소 클론**
   ```bash
   git clone https://github.com/your-username/washing-machine-reservation.git
   cd washing-machine-reservation
   ```

2. **가상 환경 생성 (선택사항)**
   ```bash
   python -m venv venv
   
   # Windows
   venv\Scripts\activate
   
   # macOS/Linux
   source venv/bin/activate
   ```

3. **필요한 패키지 설치**
   ```bash
   pip install -r requirements.txt
   ```

4. **애플리케이션 실행**
   ```bash
   python app.py
   ```

5. **웹 브라우저에서 접속**
   ```
   http://localhost:5000
   ```

## 📖 사용 방법

### 1. 사용자 이름 입력
- 페이지 상단에 이름을 입력하고 "확인" 버튼 클릭

### 2. 세탁 시작
- 세탁 시간을 입력 (기본 30분, 최대 120분)
- "세탁 시작 / 예약하기" 버튼 클릭
- 사용 가능한 세탁기가 있으면 바로 시작
- 없으면 대기 예약이 생성됨

### 3. 세탁 완료 확인
- 세탁기 카드에서 "완료" 상태 확인
- 본인의 세탁물이면 "옷 가져가기" 버튼 클릭

### 4. 알림 확인
- 세탁 완료 시 자동으로 알림이 표시됨
- 알림 섹션에서 확인 가능
- "알림 삭제" 버튼으로 삭제 가능

## 📁 파일 구조

```
washing-machine-reservation/
│
├── app.py                 # Flask 웹 애플리케이션 메인 파일
├── models.py              # 데이터 모델 (Machine, Reservation)
├── washing_system.py      # 핵심 시스템 로직
├── database.py            # SQLite 데이터베이스 관리
├── requirements.txt       # Python 패키지 의존성
├── README.md             # 프로젝트 설명서
├── .gitignore            # Git 제외 파일 목록
├── Procfile              # 호스팅 플랫폼용 설정 (Render, Heroku 등)
├── runtime.txt           # Python 버전 지정
│
├── templates/            # HTML 템플릿
│   └── index.html        # 메인 페이지
│
├── static/               # 정적 파일
│   └── style.css         # 스타일시트
│
└── washing_machine.db    # SQLite 데이터베이스 파일 (자동 생성)
```

## 🛠️ 기술 스택

- **Backend**: Python 3.7+
- **Web Framework**: Flask 3.0.0
- **Database**: SQLite (무료, 파일 기반)
- **Frontend**: HTML5, CSS3, JavaScript (Vanilla)
- **아키텍처**: MVC 패턴

### 데이터 저장
- **SQLite 데이터베이스**: 모든 데이터를 영구 저장
- 서버 재시작 후에도 데이터 유지
- 다중 사용자 동시 접근 지원
- **요금 없음**: SQLite는 무료이며 추가 서비스 불필요

## 🔧 설정 변경

### 세탁기 개수 변경
`app.py` 파일에서 세탁기 개수를 변경할 수 있습니다:

```python
washing_system = WashingMachineSystem(num_machines=5)  # 5대로 변경
```

### 세탁 시간 기본값 변경
`models.py`의 `start_washing` 메서드에서 기본값을 변경할 수 있습니다:

```python
def start_washing(self, user_name: str, duration_minutes: int = 45):  # 45분으로 변경
```

### 예약 만료 시간 변경
`models.py`의 `Reservation` 클래스에서 변경할 수 있습니다:

```python
self.expiry_time = self.reservation_time + timedelta(minutes=10)  # 10분으로 변경
```

## ☁️ 무료 호스팅 배포

이 프로젝트는 **완전 무료**로 호스팅할 수 있습니다:

### 추천 무료 호스팅 플랫폼

#### 1. Render (추천)
- **무료 티어**: 무제한 사용 가능
- **배포 방법**:
  1. GitHub 저장소 연결
  2. Build Command: `pip install -r requirements.txt`
  3. Start Command: `python app.py`
  4. 환경 변수: `FLASK_DEBUG=False`

#### 2. Railway
- **무료 크레딧**: 매월 $5 크레딧 제공
- **배포 방법**: GitHub 연결 후 자동 배포

#### 3. Fly.io
- **무료 티어**: 3개 앱까지 무료
- **배포 방법**: `flyctl launch` 명령어 사용

### 배포 시 주의사항
- SQLite 데이터베이스 파일은 호스팅 플랫폼의 파일 시스템에 저장됩니다
- 일부 플랫폼에서는 파일 시스템이 임시적일 수 있으므로, 필요시 외부 스토리지 연동 고려
- 환경 변수 `PORT`를 통해 포트 설정 (대부분의 플랫폼에서 자동 설정)

## 🔮 확장 가능성

이 프로젝트는 다음과 같이 확장할 수 있습니다:

### 1. 고급 데이터베이스 연동
- PostgreSQL, MySQL 등으로 업그레이드 (대규모 사용자용)
- 사용자 인증 시스템 추가

### 2. 모바일 앱 개발
- RESTful API를 활용하여 iOS/Android 앱 개발
- 푸시 알림 기능 추가

### 3. 하드웨어 연동
- 실제 세탁기에 센서 연결
- IoT 기기를 통한 자동 상태 감지

### 4. 추가 기능
- 세탁기 사용 통계 및 분석
- 사용자별 사용 이력 관리
- 관리자 페이지 추가

## 📝 라이선스

이 프로젝트는 교육 목적으로 제작되었습니다.

## 👥 기여

버그 리포트나 기능 제안은 이슈로 등록해주세요.

## 📧 문의

프로젝트에 대한 문의사항이 있으시면 이슈를 등록해주세요.

---

**개발자**: 고등학교 자율융합탐구 프로젝트  
**개발 목적**: 기숙사 생활의 편의성 향상  
**버전**: 1.0.0

