name: Setup Codespaces

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Setup message
        run: |
          echo "✅ Codespaces 환경이 준비되었습니다!"
          echo "터미널에서 'python app.py'를 실행하세요."

